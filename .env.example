# Environment Configuration for Edu-ECG
# Copy this file to .env and fill in the values

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_NAME=edu_ecg
DB_USER=eduecg_admin
# Generate secure password: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE

# ============================================
# REDIS CONFIGURATION
# ============================================
# Redis is used for:
# - Ontology caching (OWL graph)
# - Rate limiting tracking
# - Session management (future)

# ============================================
# OPENAI API CONFIGURATION
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-CHANGE_ME_YOUR_OPENAI_KEY

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate secure key: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_ME_SECURE_JWT_SECRET_KEY

# ============================================
# APPLICATION SETTINGS
# ============================================
ENVIRONMENT=development
# Options: development, production

# ============================================
# RATE LIMITING & QUOTAS
# ============================================
# Maximum LLM corrections per student per month
MAX_CORRECTIONS_PER_MONTH=100

# Monthly budget limit in USD (circuit breaker)
MONTHLY_BUDGET_LIMIT_USD=50

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate DB_PASSWORD: openssl rand -base64 32
# 3. Generate JWT_SECRET_KEY: openssl rand -hex 32
# 4. Add your OPENAI_API_KEY
# 5. Adjust quotas based on your needs
# 6. NEVER commit .env to git (already in .gitignore)
